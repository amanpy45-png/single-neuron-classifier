# single-neuron-classifier
A simple single-neuron binary classifier using Python and NumPy to predict student pass/fail outcomes based on CGPA and hours studied. Implements forward propagation, sigmoid activation, and gradient descent training. Demonstrates basic neural network concepts on a small dataset with interpretable results
